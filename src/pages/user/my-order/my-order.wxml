<navigation-bar title="我的订单" 
                isShowBack="{{ true }}"
                navBorderBottom="{{ false }}"></navigation-bar>
<view class='noData' wx:if="{{ listData.length == 0 }}">
  <image src='{{ bgImgUrl }}'></image>
  <view class='tip'>空空如也！</view>
  <button class='add'  bindtap="goto">去逛逛</button>
</view>
<view wx:else>
  <view class="list-view"
    wx:for="{{ listData }}"
    wx:key="{{ index }}">
    <view class="img-view">
      <image src="{{orderImg}}"></image>
    </view>
    <view class="flex1">
      <view class="titleCls">{{item.title}}</view>
      <view class="typeCls">付款方式：{{item.type}}</view>
      <view class="statusCls">发货状态：{{item.status}}</view>
      <view class="flexCls">
        <view>已付款￥{{item.price}}</view>
        <view>付款时间{{item.date}}</view>
      </view>
    </view>
  </view>
</view>